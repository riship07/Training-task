function  main(s: string, n: number, direction: "left" | "right"): any{
      console.log(direction);
      
};
var r : "lest" | "right" = "right"  // var r = "right"
main("hi", 10, r);          //you can not direct pass r because main not expect string type it expect "left"|"right" type